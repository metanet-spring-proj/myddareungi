<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"../mybatis-3-mapper.dtd">

<mapper namespace="com.metanet.myddareungi.domain.notification.repository.INotificationRepository">
	<resultMap id="NotificationResultMap" type="com.metanet.myddareungi.domain.notification.model.Notification">
        <id     property="notificationId"   column="NOTIFICATION_ID" />
        <result property="userId"           column="USER_ID" />
        <result property="notificationType" column="NOTIFICATION_TYPE" />
        <result property="message"          column="MESSAGE" />
        <result property="fileId"           column="FILE_ID" />
        <result property="isRead"           column="IS_READ" />
        <result property="createdAt"        column="CREATED_AT" />
    </resultMap>
    <!--  알림 목록 조회  -->
	<select id="findAllByUserId" resultMap="NotificationResultMap"> SELECT NOTIFICATION_ID, USER_ID, NOTIFICATION_TYPE, MESSAGE, FILE_ID, IS_READ, CREATED_AT FROM NOTIFICATION WHERE USER_ID = #{userId} ORDER BY CREATED_AT DESC </select>
	<!--  알림 읽음 처리  -->
	<update id="markAsRead"> UPDATE NOTIFICATION SET IS_READ = 1 WHERE NOTIFICATION_ID = #{notificationId} </update>
	<!--  알림 삭제  -->
	<delete id="deleteById"> DELETE FROM NOTIFICATION WHERE NOTIFICATION_ID = #{notificationId} </delete>
</mapper>