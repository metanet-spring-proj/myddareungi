<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.metanet.myddareungi.domain.member.repository.MemberMapper">

    <select id="findByLoginId" parameterType="string" resultType="com.metanet.myddareungi.domain.member.model.Member">
        SELECT
            USER_ID,
            LOGIN_ID,
            PASSWORD,
            USER_NAME,
            EMAIL,
            ROLE,
            CREATED_AT,
            UPDATED_AT,
            QUESTION,
            ANSWER
        FROM USERS
        WHERE LOGIN_ID = #{loginId}
    </select>

    <select id="findByEmail" parameterType="string" resultType="com.metanet.myddareungi.domain.member.model.Member">
        SELECT
            USER_ID,
            LOGIN_ID,
            PASSWORD,
            USER_NAME,
            EMAIL,
            ROLE,
            CREATED_AT,
            UPDATED_AT,
            QUESTION,
            ANSWER
        FROM USERS
        WHERE EMAIL = #{email}
    </select>

    <select id="findByLoginIdAndEmail" resultType="com.metanet.myddareungi.domain.member.model.Member">
        SELECT
            USER_ID,
            LOGIN_ID,
            PASSWORD,
            USER_NAME,
            EMAIL,
            ROLE,
            CREATED_AT,
            UPDATED_AT,
            QUESTION,
            ANSWER
        FROM USERS
        WHERE LOGIN_ID = #{loginId}
          AND EMAIL = #{email}
    </select>

    <select id="countUsers" resultType="int">
        SELECT COUNT(*)
        FROM USERS
    </select>

    <insert id="insertMember" parameterType="com.metanet.myddareungi.domain.member.model.Member">
        INSERT INTO USERS (
            LOGIN_ID,
            PASSWORD,
            USER_NAME,
            EMAIL,
            ROLE,
            QUESTION,
            ANSWER
        ) VALUES (
            #{loginId},
            #{password},
            #{userName},
            #{email},
            #{role},
            #{question},
            #{answer}
        )
    </insert>

    <update id="updatePasswordByUserId">
        UPDATE USERS
        SET PASSWORD = #{password},
            UPDATED_AT = CURRENT_TIMESTAMP
        WHERE USER_ID = #{userId}
    </update>

</mapper>
